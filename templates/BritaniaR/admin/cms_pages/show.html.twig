{% extends 'base.html.twig' %}

{% block title %}CmsPages{% endblock %}

{% block body %}
    <h1>CmsPages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cms_page.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ cms_page.category }}</td>
            </tr>
            <tr>
                <th>UniqueSlug</th>
                <td>{{ cms_page.uniqueSlug }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cms_page.name }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ cms_page.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cms_pages_index') }}">back to list</a>

    <a href="{{ path('cms_pages_edit', {'id': cms_page.id}) }}">edit</a>

    {{ include('cms_pages/_delete_form.html.twig') }}
{% endblock %}

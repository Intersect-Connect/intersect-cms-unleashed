{{ form_start(form) }}

<div class="form-group mt-3 mb-3">
    <label for="">{{"Titre de la page"|trans}}</label>
    {{ form_widget(form.name) }}
</div>

<div class="form-group mt-3 mb-3">
    <label for="">{{"Catégorie de la page"|trans}}</label>
    {{ form_widget(form.category) }}
</div>
<div class="form-group mt-3 mb-3">
    <label for="">{{"Contenu de la page"|trans}}</label>
    {{ form_widget(form.content) }}
</div>

<div class="form-check mt-3 mb-3">
   {{ form_widget(form.is_visible) }}
    <label forcms_pages_is_visible class="form-check-label">{{"Page visible ?"|trans}}</label>
</div>

<button class="btn btn-success">{{"Sauvegarder"|trans}}</button>
{{ form_end(form) }}
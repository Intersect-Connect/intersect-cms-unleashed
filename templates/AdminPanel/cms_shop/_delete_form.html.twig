<form method="post" action="{{ path('cms_shop_delete', {'id': cms_shop.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cms_shop.id) }}">
    <button class="btn btn-danger btn-block mt-3">{{"Supprimer"|trans}}</button>
</form>

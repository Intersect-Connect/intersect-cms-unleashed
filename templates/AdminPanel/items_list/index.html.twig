{% extends 'AdminPanel/base.html.twig' %}

{% block title %}{{"Liste des items"|trans}}{% endblock %}

{% block body %}

<div class="card shadow mb-4">
  <div class="card-header py-3">
    <h6 class="m-0 font-weight-bold text-primary">{{'Liste des items'|trans}}</h6>
  </div>
  <div class="card-body">

    <div class="table-responsive">
      <table class="ladder table table-striped py-3">
        <thead>
          <tr>
            <th>{{"Id de l'item"|trans}}</th>
            <th class="c">{{"Nom de l'item"|trans}}</th>
          </tr>
        </thead>
        <tbody>
          {% for item in items %}
          <tr>
            <td class="item_id">
              {{ item.Value.Id }}
            </td>
            <td class="c">{{ item.Value.Name }}</td>
          </tr>
          {% else %}
          <tr class="border-0">
           <td colspan="12" class="p-0 border-0"> 
              <div class="alert alert-info mb-0 rounded-0">
                 Aucune information disponible.
              </div>
           </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>      

      <nav aria-label="Pagination de la liste des joueurs" class="{% if items|length == 0 %}d-none{% endif %}">
        <ul class="pagination p-3 overflow-auto">
          {% for i in 0..total_page %}
          <li class="page-item  {% if page_actuel == i %}active{% endif %}">
            <a class="page-link" data-page="{{i}}" href="{{path('admin.items', {"page":i})}}">{{ i + 1}}</a>
          </li>
          {% endfor %}
        </ul>
      </nav>
    </div>
  </div>
</div>
{% endblock %}
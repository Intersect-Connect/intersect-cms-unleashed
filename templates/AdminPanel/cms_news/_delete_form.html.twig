<form method="post" class="mt-n3"action="{{ path('admin.news.delete', {'id': cms_news.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cms_news.id) }}">
    <button class="btn btn-danger mt-4 btn-block">{{"Supprimer"|trans}}</button>
</form>
